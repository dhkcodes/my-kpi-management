# Accounts & Workloads Compact 데이터 모델 요구사항

> 상태: Compact 5-table ADB 배포 및 FY27 Excel migration 완료
> 기준 원본: `(FY27) Accounts&Workloads (Jooseok).xlsx`의 `Deal Status`
> 범위: Accounts & Workloads, Home Executive Pulse, My Customers 360를 지원하는 Oracle Autonomous Database 모델

## 1. 목표와 범위

현재 UI의 한 행을 FY별 Commitment로 관리하면서 다음 5개 테이블만 핵심 런타임 모델로 사용한다.

1. `AW_ACCOUNT`
2. `AW_WORKLOAD`
3. `AW_COMMITMENT`
4. `AW_FX_RATE`
5. `AW_COMMITMENT_UPDATE`

Account와 Workload는 안정적인 식별·중복 방지·Account별 집계를 위해 분리한다. 독립 lifecycle이 없는 Opportunity는 `AW_COMMITMENT.opportunity_no`로 통합한다. Person, 공통 Code, Fiscal Period, Load Batch 테이블은 현재 범위에서 제거한다.

Spring Boot `adb` profile의 실제 ADB 연결과 readiness 검증은 완료했다. 이번 범위에서 목록/필터/정렬, 로우 생성, 변경 필드 PATCH, soft delete, Home 통계 API를 구현하고 JET adapter를 연동한다. 별도 애플리케이션 사용자/GRANT와 컬럼 변경 로그 테이블은 후속 범위다.

## 2. 확인 근거

- `src/components/content/AccountsWorkloadsPage.tsx`: 상세 컬럼, 편집, Add Row, Highlight, soft delete, 환율
- `src/components/content/AccountsWorkloadsPulseV2.tsx`: Home KPI와 urgency
- `src/data/accountsWorkloadsPulseV2.ts`: KPI, Account 집계, New Commit/Renewal 규칙
- `src/components/content/MyCustomers360Page.tsx`: Account Portfolio
- `src/data/accountsWorkloadsDataSource.ts`: FY별 공유 데이터
- 실제 Excel `Deal Status`: Account, Workload, Plan, Opportunity, 날짜, ARR/ACR, Target, Win Probability, Status Update, Notes, 환율

## 3. UI 및 업무 필드

| 화면/원본 필드 | 저장 위치 | 규칙 |
|---|---|---|
| Account | `AW_ACCOUNT.account_name` | 필수, trim/공백/대소문자 정규화 중복 방지 |
| Workload Name (Enduser) | `AW_WORKLOAD.workload_name` | 필수, 동일 Account 안에서 정규화명 unique |
| Plan Number | `AW_COMMITMENT.plan_number` | 선택 |
| Oppty.No | `AW_COMMITMENT.opportunity_no` | 선택, 현재 원본의 복수 표기도 문자열로 보존 |
| FY | `AW_COMMITMENT.fiscal_year` | 현재 화면의 한 행은 FY별 1건 |
| Start/End Date | `AW_COMMITMENT.start_date/end_date` | Oracle `DATE`, 둘 다 있으면 End ≥ Start |
| ARR($)/(₩) | `arr_usd/arr_krw` | 음수 불가, USD 2자리·KRW 정수 |
| ACR($)/(₩) | `acr_usd/acr_krw` | 음수 불가, USD 2자리·KRW 정수 |
| 입력 원본 통화 | `arr_source_currency/acr_source_currency` | `USD`, `KRW`, `MIGRATED`; 금액이 있으면 필수 |
| 적용 환율 | `fx_rate_id` | `AW_FX_RATE` provenance |
| Target | `target_fiscal_year/target_quarter/target_period_end_date` | 함께 NULL 또는 함께 입력, `FYnn` + `Q1..Q4` + UI 회계달력 기준 분기 말일 |
| Win Prob. | `win_probability` | 0~100 |
| 상태 | `status_code` | 작은 enum을 CHECK로 보호 |
| Commitment 유형 | `commitment_type_code` | Excel에서 추정하지 않고 `UNCLASSIFIED` 허용 |
| Highlight | `is_important` | `Y/N`; Excel의 Account/Workload/Oppty 노란색 채움은 `Y` |
| Latest Update | `AW_COMMITMENT_UPDATE.update_text` | 덮어쓰지 않고 append-only |
| Notes | `AW_COMMITMENT.notes` | 헤더 없는 O열 값 1건을 기존 UI 의미에 따라 원문 보존 |
| 삭제 | `deleted_at/deleted_by` | 논리 삭제, pair 일관성 |
| 동시성 | `version_no` | optimistic locking, 1 이상 |
| 원본 추적 | workbook hash/sheet/row | 실데이터를 Git에 넣지 않고 DB 행 provenance만 저장 |

## 4. Excel 원본 검증 결과

### 4.1 구조

- 시트: `Deal Status`, `Consumption`, `Business Weekly (FY27 Q1)`
- Migration 대상: `Deal Status`만 사용
- `Deal Status`: 31행 × 16열, 헤더 2행, 후보 데이터 29행
- 금액 Formula: 41개, cached error 0건
- 적용 환율: USD→KRW `1506.1`
- 수식 방향:
  - USD 셀 `=KRW/환율`: KRW가 authoritative
  - KRW 셀 `=USD*환율`: USD가 authoritative
- 날짜: Excel 날짜 셀 22건씩, `d-mmm-yy`와 `mm-dd-yy` 표시 형식
- Win Probability: `0%` 형식의 원본 값 `1`을 100%로 매핑

`Consumption`은 유효 업무 데이터가 3개 셀뿐이고, `Business Weekly`는 주간 메모이므로 Compact 5-table 적재 대상에서 제외한다.

### 4.2 Dry-run 결과

| 항목 | 결과 |
|---|---:|
| 후보 Commitment | 29 |
| 검증 통과/적재 대상 | 26 |
| 제외 | 3 |
| 원본 Account distinct | 9 |
| Account | 8 |
| Workload | 26 |
| FX Rate | 1 |
| Update 이력 | 25 |
| Important | 5 |
| Target 입력 | 8 |
| Account+Workload 중복 그룹 | 0 |
| Plan Number 중복 그룹 | 0 |
| Opportunity 표기 중복 그룹 | 0 |

제외 사유는 원본 행 번호로만 보고한다.

- 원본 행 18: End Date가 Start Date보다 빠름
- 원본 행 21: End Date가 Start Date보다 빠름
- 원본 행 27: Workload Name 누락

원본을 임의 보정하지 않고 3행을 제외한다. 재적재하려면 원본 수정 또는 승인된 보정 규칙이 필요하다.

### 4.3 금액 변환 규칙

1. Formula 반대편 literal 또는 유일하게 입력된 통화를 authoritative로 판정한다.
2. USD authoritative: USD를 소수 2자리로 반올림하고 KRW는 `ROUND(USD × 1506.1, 0)`.
3. KRW authoritative: KRW를 정수로 반올림하고 USD는 `ROUND(KRW ÷ 1506.1, 2)`.
4. 둘 다 Formula가 아니면서 값이 있으면 `MIGRATED`; 이번 원본에는 해당 적재 행이 없다.
5. 둘 다 NULL이면 금액과 source currency 모두 NULL이다.
6. 기존 Formula cached pair는 모두 환율 기준 오차 1 KRW 이내였다.
7. 원본에서 반대 통화가 비어 있던 ARR 3건, ACR 1건은 위 공식으로 파생값을 채운다.
8. Formula cached 소수값은 저장 precision에 맞게 USD 2자리, KRW 정수로 재계산한다.

변환 후 적재 기대 합계:

| 합계 | 기대값 |
|---|---:|
| ARR USD | 1,447,659.34 |
| ARR KRW | 2,180,319,712 |
| ACR USD | 1,396,677.03 |
| ACR KRW | 2,103,535,250 |

## 5. 데이터 무결성

- `AW_ACCOUNT`: normalized Account name unique
- `AW_WORKLOAD`: `(account_id, normalized workload name)` unique
- `AW_COMMITMENT`: `(workload_id, fiscal_year)` unique
- 원본 중복 방지: `(source_workbook_hash, source_sheet, source_row_number)` unique
- 모든 FK는 ENABLED/VALIDATED 상태여야 한다.
- 금액은 0 이상, 환율은 0 초과다.
- Target FY/Quarter/period end는 세 컬럼을 함께 입력한다.
- `fx_rate_id`와 Commitment `fiscal_year`는 composite FK로 동일 FY 환율만 참조한다.
- source currency가 USD/KRW이면 해당 authoritative 금액 컬럼이 반드시 존재해야 한다.
- `deleted_at`과 `deleted_by`는 함께 NULL 또는 함께 값이 있어야 한다.
- `version_no >= 1`이다.
- Opportunity No는 현재 복수 표기가 있어 전역 unique를 강제하지 않는다.
- Latest Update는 `AW_COMMITMENT_UPDATE`에 append하고 Commitment의 `updated_at/by`, `version_no`를 같은 트랜잭션에서 갱신한다.

## 6. 부분 PATCH 및 동시성

현재 UI의 draft/saved 비교 결과로 변경된 필드만 PATCH payload에 포함한다. API 구현은 다음을 지킨다.

```sql
UPDATE aw_commitment
   SET <요청에 존재하는 허용 컬럼만>,
       updated_at = SYSTIMESTAMP,
       updated_by = :actor,
       version_no = version_no + 1
 WHERE commitment_id = :commitment_id
   AND version_no = :old_version
   AND deleted_at IS NULL;
```

- 0행 갱신은 `HTTP 409 Conflict`, `VERSION_CONFLICT`로 처리한다.
- 미전달과 명시적 NULL을 구분한다.
- `NVL(:value, column)`/`COALESCE(:value, column)` 방식은 사용하지 않는다.
- Target FY+Quarter, ARR/ACR+source currency+FX, 삭제 actor/time, 감사/version은 각각 원자적 그룹이다.

## 7. Dashboard 집계

### KPI

- Active Accounts: 활성 FY Commitment를 가진 distinct Account 수
- Active Workloads: 활성 FY Commitment 수
- ARR/ACR: 활성 FY Commitment USD 합계
- Important: `is_important='Y'`
- Target Coverage: Target FY/Quarter가 입력된 활성 행 수와 비율

### Urgency

- Renewal/Expand: End Date가 있는 활성 행, `days_remaining=end_date-as_of_date`
- New Commit fallback: Start 또는 End Date가 없는 활성 행, 저장된 Target quarter end 기준
- Critical ≤90일, Attention 91~180일, Upcoming 181~270일
- overdue는 해결/삭제 전까지 Critical
- Top Action 정렬: `days_remaining ASC, account_id, workload_id`

현재 UI 회계달력은 FY가 6월 1일 시작하고 Q1/Q2/Q3/Q4가 각각 8월/11월/2월/5월 말일에 종료하는 규칙이다. Compact 모델은 이 계산 결과를 `target_period_end_date`로 저장한다. 회계달력을 DB 기준정보로 독립 운영하거나 변경 이력을 관리해야 하면 `AW_FISCAL_PERIOD`를 재도입한 6-table 모델로 확장한다.

### My Customers 360

Account별 Workload 수, ARR, ACR, Targeted/Workloads, Important를 동일 활성 데이터에서 집계한다.

## 8. 보안 및 운영

- 실제 Account, Opportunity, 메모 및 행 전체를 Slack·로그·public Git에 출력하지 않는다.
- Excel 원본과 migration payload는 private cache/임시 경로에서만 처리한다.
- 비밀번호, OAuth token, Bearer, client secret, 연결 문자열을 출력·저장하지 않는다.
- Oracle 작업은 이 채널에서 `oracle_adb`의 `myoracle` MCP만 사용한다.
- `oracle_adb_voxbridgeai`는 사용하지 않는다.
- Managed MCP의 `SELECT USER` 값을 OAuth 주체 증거로 사용하지 않는다.
- 임의 GRANT/IAM/네트워크 변경은 범위 밖이다.
- Commit/Push/PR은 별도 지시 없이는 수행하지 않는다.

## 9. API 연동 범위

- Backend list/search/sort/filter API
- 로우 생성, 컬럼 단위 PATCH와 409 UI
- Latest Update append API
- Soft Delete·Restore·완전 삭제 API
- UI API adapter와 synthetic 개발 fallback
- Home 통계 projection 연동

다음은 후속 범위다: FX Apply API, 선택적 `AW_CHANGE_LOG`, 인증/권한 UI, 최소 권한 애플리케이션 DB 사용자/GRANT.

### 9.1 이번 브랜치의 완료 기준과 제외 범위

- JET는 FY·검색·정렬·Include deleted 변경 때 목록 API를 다시 조회하며, 편집 중에는 해당 query control을 잠가 draft 유실을 막는다.
- fallback은 loopback 개발 환경에서 fetch transport 실패 또는 endpoint 404일 때만 허용한다. 401/403/5xx·JSON decode/row validation 오류·명시적 API 설정 실패는 오류로 표시한다.
- 다중 행 저장은 changed row만 mutation 대상으로 관리한다. 일부 성공 뒤 실패하면 서버 상태를 재조회해 성공 mutation을 제거한 retry draft만 보존하며, 재조회 자체가 실패해도 성공 응답으로 합성한 상태를 기준으로 이미 완료된 mutation을 재전송하지 않는다.
- 저장 시 신규 행은 POST, 기존 행은 saved/draft diff 기반 changed-fields-only PATCH, 삭제 행은 version을 포함한 soft DELETE를 호출한다.
- 저장 중에는 화면 중앙의 작은 Oracle JET `oj-dialog` 안에 indeterminate `oj-progress-circle`을 표시하고 입력을 차단한다.
- Notes는 셀 내부에서 줄바꿈하고 4줄 높이로 제한해 긴 텍스트가 테이블 밖으로 넘치지 않게 한다.
- 단일 삭제 버튼은 활성 행에서 Draft 삭제를, 이미 Draft 삭제된 행에서 완전 삭제를 수행한다. 완전 삭제 전에는 중앙의 작은 JET 경고 dialog로 재확인한다.
- Draft 삭제된 행은 Restore로 복원하며, 수정·Draft 삭제·복원 중 선택 상태를 유지하고 최종 Save 성공 뒤에만 선택을 해제한다.
- Home Accounts & Workloads 통계는 API에서 재조회한 공용 saved rows를 동일 계산 모듈에 전달해 상세 화면과 일치시킨다.
- Dashboard summary endpoint/adapter는 제공하지만, 현재 Home 렌더링은 별도 summary 응답이 아니라 공용 saved rows를 사용한다.
- 완료 검증은 credential 없는 FE 단위 테스트·JET build와 local profile 실제 HTTP 통합 테스트까지다.
- ADB schema/data 변경, ADB write smoke test, commit/push/PR은 이번 검증 범위에서 제외한다.

## 10. 실제 ADB 배포 결과

- 대상 연결/스키마: `oracle_adb` 용도의 `MYORACLE` 계정과 동일한 ADB Wallet/JDBC 환경
- 사전 충돌: 대상 5개 테이블 0, 대상 행 0, 기타 `AW_%` 객체 0
- 생성: Table 5, 명시적 Index 7, migration 함수 `AW_LOAD_BATCH_V1` 1
- 제약조건: PK 5, FK 4, UK 6, 명시적 CHECK 26; 전체 ENABLED/VALIDATED
- 적재: Account 8, Workload 26, FX 1, Commitment 26, Commitment Update 25
- 제외: 원본 행 18/21의 날짜 역전, 원본 행 27의 Workload 누락 등 기존 dry-run 3행
- 중복, 필수 NULL, 날짜 순서, Target triple, 통화 provenance, FK orphan: 모두 0
- 화면 조회: FY27 26행, Latest Update 보유 25행
- Home: Active Accounts 8, Active Workloads 26, Targeted 8, Important 5
- Spring Boot: Java 21 `clean verify` 11 tests 통과, 실제 `adb` profile readiness HTTP 200/UP

`AW_LOAD_BATCH_V1`은 재적재 시 empty-table guard로 차단되며 MCP Tool로 등록하지 않았다. 삭제는 별도 승인 전까지 수행하지 않는다.

## 배포 화면 반영 요구사항 (2026-08-13)
- Tailnet 실제 서비스 화면에서 Notes는 셀 너비 안에서 줄바꿈하고 최대 4줄로 제한하며 가로·세로 overflow를 차단한다.
- 수정 Save의 비동기 처리 동안 중앙 소형 Oracle JET `oj-dialog`와 indeterminate `oj-progress-circle`를 실제 표시한다.
- Draft 삭제·완전 삭제 단일 버튼, 경고 dialog, Restore, Save 성공 시 선택 해제를 배포 산출물에서 검증한다.
- Tailnet-only 접근을 유지하며 애플리케이션 인증은 이번 범위에서 추가하지 않는다.

## URL 라우팅 및 새로고침 요구사항 (2026-08-13)
- Accounts & Workloads는 `/accounts-workloads` 고유 경로를 사용한다.
- 메뉴 이동은 브라우저 History API에 반영되어 뒤로·앞으로 이동으로 화면 상태를 복원한다.
- 일반 새로고침, 강력 새로고침, URL 직접 입력 후에도 동일 화면을 유지한다.
- 기존 `#pipeline` URL은 `/accounts-workloads`로 정규화해 하위 호환한다.
- Tailnet-only Tailscale Serve 프록시와 loopback 정적 서버는 extensionless SPA 경로를 `index.html`로 fallback한다. 실제 정적 asset 404는 fallback하지 않는다.

## Accounts & Workloads UX 보완 요구사항 (2026-08-13)

- 최근 History routing의 직접 URL 초기 선택이 Oracle JET Sliding navigation 내부 drill 상태와 불일치를 만들어 왼쪽 메뉴가 튀는 체감이 발생하므로 해당 History routing·SPA fallback 변경만 원복한다.
- 원복 후 메뉴 이동은 기존 hash/in-memory 방식으로 동작하며 새로고침하면 Home으로 복귀한다.
- 상단 삭제 버튼 라벨은 선택 상태와 관계없이 항상 `Delete`로 표시한다.
- 정상 행은 Draft 삭제, 이미 Draft 삭제된 행은 완전 삭제하며 혼합 선택은 한 번의 Delete에서 두 규칙을 함께 적용한다.
- 완전 삭제 대상이 하나라도 있으면 중앙 Oracle JET 확인 dialog를 한 번 표시한다.
- `Restore`는 선택한 행 중 Draft 삭제 상태가 하나라도 있을 때만 표시한다.
- 최초 조회 및 Refresh 조회 중에는 indeterminate `oj-progress-circle`과 접근성 `aria-busy` 상태를 표시한다.
- `Refresh`는 편집 중이 아닐 때 현재 FY·검색·Include deleted·정렬 조건으로 서버 데이터를 다시 조회한다.
- `Selected rows: N` 문구는 표시하지 않는다.
- Notes는 한 줄 말줄임으로 셀 경계를 넘지 않으며 hover 시 `title` tooltip으로 전체 내용을 확인할 수 있어야 한다.
- 실제 ADB 완전 삭제 smoke test는 수행하지 않는다.

## Weekly Activities 구현 요구사항 (2026-08-15)

- 최종 UX 기준은 Obsidian `fddf798` 및 image SHA-256 `c29e060c86d3bdf867fd2bdc6d4fdbc4be7c46865243ea81112cc8aa30c23618`이다.
- `/weekly-activities`에서 Adaptive Dual-Section Cards를 `weekOfDate DESC`로 조회하고 From/To calendar, 내용 검색, Reset, 기본 50건·최대 100건 pagination을 제공한다.
- readonly Desktop은 Completed/Planned 2열, iPhone은 `Completed ↓ Planned` 세로 흐름이다.
- Add/Edit는 선택 card 아래 Inline Row Detail이며 `Week Date → Toolbar 1개 → Target Selector 1개 → Editor 1개/Preview → Save Row·Cancel` 순서다.
- Quill 2 app-owned wrapper가 하나의 toolbar와 editor 인스턴스를 소유한다. target 전환 전 현재 HTML을 draft에 flush하고 상대 draft를 같은 editor에 load한다.
- allow-list는 size 12/14/16/18/20px, bold, 제한 color, ordered/unordered list이며 preview에는 server-sanitized canonical HTML만 렌더링한다.
- Desktop 1440px와 iPhone 390px에서 View/Add/Edit/Cancel/Search/error, 직접접속·새로고침 및 기존 KAP 회귀를 검증한다.
- 운영 배포는 이 branch 범위 밖이다.

## Weekly Activities 구현 결과 (2026-08-15)
- 상태: Frontend 구현 및 로컬 검증 완료, Backend 통합 검증 대기
- 구현: Adaptive Dual-Section Cards, inclusive From/To·내용 검색, Calendar Week Date, Inline Add/Edit/Cancel, 페이지 추가 로드
- Editor: Quill 2 shared instance 1개, Toolbar 1개, Target Selector 1개, inactive read-only preview
- 반응형: Desktop 2열, iPhone active editor → preview stack 및 Completed ↓ Planned cue
- 보안 경계: 조회 HTML은 Backend canonical sanitized HTML을 렌더링하고 최종 저장 검증·sanitize는 Backend가 담당
